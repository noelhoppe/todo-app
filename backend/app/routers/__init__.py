"""
This package contains all API route definitions (routers) for the application.

Each module inside this package defines a group of related routes,
organized by feature (e.g., authentication, todo management).

These routers are included in the FastAPI application using `include_router`.

Example:
    from app.routes import auth, todo
"""